# Program 5 - skipped (same pattern as 3 & 4, user skipped)
# length = int(input("enter length: "))
# breadth = int(input("enter breadth: "))
# area = length * breadth
# perimeter = 2 * (length + breadth)
# print(f"area = {area}")
# print(f"perimeter = {perimeter}")
